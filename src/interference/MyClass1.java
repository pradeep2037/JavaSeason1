package interference;

public interface MyClass1 {
	
//	if we declare variable inside interface by default it will consider static final
	
	double p = 3.14;
	
//	if we declare method inside interfcae by default it will consider abstract method
	void m1(int a);
}
